#INCLUDE "protheus.ch"

user function aula2()
local cTexto := "Variavel do tipo caracter"  // local = so vai estar visivel dentro da função
local nNumero := 0
local dDate := CTOD("01/05/2023") // A função CTOD transforma o caracter dentro do parametro em data
local lLogical := .T.
local aLista := {"Caio Rafael"}
local xNull := nil

alert(cTexto)

return


